1. В методе `end_with` опечатка в названии метода, должно быть `ends_with` вместо `end_with`.
2. В методе 'capitilize' опечатка в названии метода в возвращении версии строки, должно быть `capitilize` вместо `capitalize`.
